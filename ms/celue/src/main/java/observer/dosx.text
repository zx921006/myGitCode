观察者模式
    实例：在我们的项目当中，需要调用第三方接口，如视频的接口，视频云会给我们流上下线回调，录制回调等等，在这里我们用到了观察者
模式，我们先定义一个观察者类实现一个动作接口（里面就一个接收事件的接口），我们在接收到回调事件的时候，观察者就会根据事件本身来
处理对应的业务逻辑，如改变我们库里面的流状态和房间状态，为什么不直接在回调事件里面修改数据库呢，是为了遵循开闭原则，以后拓展的
时候新增一些事件，我们只需要相应的增加对应的观察者来执行事件响应后的业务逻辑，不用在原来的业务代码里面进行更改